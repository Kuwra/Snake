Gra Snake napisana w C++ z biblioteką SFML.

Wymaga:
- CMake >= 3.11
- Kompilator C++ 
- SFML 2.6

Build:
mkdir build && cd build
cmake ..
cmake --build . --config Release

Uruchomienie:
./Snake.exe # Windows
./Snake # Linux/macOS
